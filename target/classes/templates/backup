<!DOCTYPE html>
<html lang="fr" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Liste des Candidats</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .admin-sidebar {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            position: fixed;
            width: 16.666667%;
        }
        .nav-link.active {
            background-color: rgba(255,255,255,0.2);
            border-radius: 8px;
        }
        .candidate-card {
            border-left: 4px solid #0d6efd;
            transition: transform 0.2s;
        }
        .candidate-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .main-content {
            margin-left: 16.666667%;
            width: 83.333333%;
        }
        .search-form {
            max-width: 300px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/">
                <i class="fas fa-briefcase me-2"></i>Portal Job - Admin
            </a>
            <a href="/" class="btn btn-outline-light">
                <i class="fas fa-home me-1"></i>Retour au site
            </a>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 admin-sidebar p-3">
                <div class="text-white mb-4">
                    <h5><i class="fas fa-cog me-2"></i>Administration</h5>
                </div>
                <nav class="nav flex-column">
                    <a class="nav-link text-white" href="/admin/personnel">
                        <i class="fas fa-users me-2"></i>Personnel
                    </a>
                    <a class="nav-link text-white active" href="/admin/candidates">
                        <i class="fas fa-user-tie me-2"></i>Candidats
                        <span class="badge bg-light text-primary ms-2" th:text="${candidates != null ? candidates.size() : 0}">0</span>
                    </a>
                    <a class="nav-link text-white" href="/admin/interviews">
                        <i class="fas fa-clipboard-check me-2"></i>Entretiens
                    </a>
                    <a class="nav-link text-white" href="/admin/results">
                        <i class="fas fa-trophy me-2"></i>Résultats
                    </a>
                    <a class="nav-link text-white" href="/admin/publish">
                        <i class="fas fa-plus-circle me-2"></i>Publier une offre
                    </a>
                </nav>
            </div>

            <!-- Main Content -->
            <div class="col-md-10 main-content">
                <div class="p-4">
                    <!-- Header avec recherche -->
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h2 class="mb-0">
                            <i class="fas fa-user-tie me-2 text-primary"></i>Liste des Candidats
                        </h2>
                        
                        <!-- Formulaire de recherche -->
                        <form th:action="@{/admin/candidates}" method="get" class="d-flex search-form">
                            <div class="input-group">
                                <input type="text" name="search" class="form-control" 
                                       placeholder="Rechercher un candidat..." 
                                       th:value="${param.search}"
                                       aria-label="Rechercher">
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-search"></i>
                                </button>
                                <a th:href="@{/admin/candidates}" class="btn btn-outline-secondary" 
                                   th:if="${param.search != null}">
                                    <i class="fas fa-times"></i>
                                </a>
                            </div>
                        </form>
                    </div>

                    <!-- Statistiques -->
                    <div class="row mb-4">
                        <div class="col-md-3">
                            <div class="card bg-primary text-white">
                                <div class="card-body text-center">
                                    <h5 class="card-title" th:text="${candidates != null ? candidates.size() : 0}">0</h5>
                                    <p class="card-text">Candidats total</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="card bg-success text-white">
                                <div class="card-body text-center">
                                    <h5 class="card-title" th:text="${candidatesWithAppliances}">0</h5>
                                    <p class="card-text">Avec candidatures</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Liste des candidats -->
                    <div class="row" th:if="${candidates != null and !candidates.empty}">
                        <div class="col-md-4 mb-3" th:each="candidate : ${candidates}">
                            <div class="card candidate-card h-100">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                        <h6 class="card-title mb-0" th:text="${candidate.prenom + ' ' + candidate.nom}">
                                            Nom Prénom
                                        </h6>
                                        <span class="badge bg-info" th:text="${candidate.age + ' ans'}">30 ans</span>
                                    </div>
                                    
                                    <!-- Poste actuel -->
                                    <p class="card-text text-muted small mb-2" 
                                       th:if="${candidate.appliances != null and !candidate.appliances.empty}">
                                        <i class="fas fa-briefcase me-1"></i>
                                        <span th:text="${candidate.appliances[0].offer.titre}">Poste</span>
                                    </p>
                                    <p class="card-text text-muted small mb-2" 
                                       th:unless="${candidate.appliances != null and !candidate.appliances.empty}">
                                        <i class="fas fa-briefcase me-1"></i>
                                        Aucune candidature
                                    </p>
                                    
                                    <!-- Contact -->
                                    <p class="small mb-2">
                                        <i class="fas fa-envelope me-1 text-primary"></i>
                                        <span th:text="${candidate.contact}" class="text-break">contact@email.com</span>
                                    </p>
                                    
                                    <!-- Dernière candidature -->
                                    <p class="small text-muted mb-3" 
                                       th:if="${candidate.appliances != null and !candidate.appliances.empty}">
                                        <i class="fas fa-calendar me-1"></i>
                                        Dernière candidature: 
                                        <span th:text="${#temporals.format(candidate.appliances[0].createdAt, 'dd/MM/yyyy')}">
                                            14/09/2025
                                        </span>
                                    </p>
                                    
                                    <!-- Actions -->
                                    <div class="d-flex gap-2">
                                        <a th:href="@{'/admin/candidate-detail?id=' + ${candidate.id}}" 
                                           class="btn btn-sm btn-primary flex-fill">
                                            <i class="fas fa-eye me-1"></i>Voir détail
                                        </a>
                                        <a th:href="@{'mailto:' + ${candidate.contact}}" 
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-envelope"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Message si aucun candidat -->
                    <div th:if="${candidates == null or candidates.empty}">
                        <div class="alert alert-info text-center">
                            <i class="fas fa-info-circle fa-2x mb-3"></i>
                            <h5>Aucun candidat trouvé</h5>
                            <p th:if="${param.search != null}">
                                Aucun résultat pour la recherche: "<span th:text="${param.search}"></span>"
                            </p>
                            <p th:unless="${param.search != null}">
                                Aucun candidat n'a été enregistré pour le moment.
                            </p>
                            <a th:href="@{/admin/candidates}" class="btn btn-primary mt-2" th:if="${param.search != null}">
                                Voir tous les candidats
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>